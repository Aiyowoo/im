CMAKE_MINIMUM_REQUIRED(VERSION 3.10)

PROJECT(comm CXX)

ADD_LIBRARY(comm status.cpp status.hpp)

INSTALL(TARGETS comm DESTINATION ${CMAKE_INSTALL_PREFIX}/lib/comm)
INSTALL(FILES status.hpp DESTINATION ${CMAKE_INSTALL_PREFIX}/include/comm)